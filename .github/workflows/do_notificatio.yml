name: "Do Actions Status Notifier"
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  do-notification:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v3
      - name: Checkout code
        uses: actions/checkout@v3
        shell: bash

      - name: Print current directory
        run: pwd
        shell: bash

      - name: List directory contents
        run: ls -la
        shell: bash

      - name: List directory contents of cmd
        run: ls -la cmd/
        shell: bash

      - name: Show go.mod file
        run: cat go.mod
        shell: bash

      - name: Run action
        if: Always()
        uses: taiki-nd/actions_notification@v0.0.9
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
